apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data_aggregator.fullname" . }}-test-connection"
  labels:
    {{- include "data_aggregator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "data_aggregator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
