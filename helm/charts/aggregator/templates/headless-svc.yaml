apiVersion: v1
kind: Service
metadata:
  name: {{ include "aggregator.fullname" . }}-headless
  labels:
    {{- include "aggregator.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aggregator.selectorLabels" . | nindent 4 }}
