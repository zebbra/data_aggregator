apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "aggregator.fullname" . }}
  labels:
    {{- include "aggregator.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: {{ include "aggregator.serviceAccountName" . }}
roleRef:
  kind: Role
  name: {{ include "aggregator.fullname" . }}
  apiGroup: rbac.authorization.k8s.io
