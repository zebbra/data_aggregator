defmodule DataAggregator.Repo.Migrations.AddStateToImageUpload do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:image_uploads) do
      add :state, :text, null: false, default: "new"
    end
  end

  def down do
    alter table(:image_uploads) do
      remove :state
    end
  end
end
