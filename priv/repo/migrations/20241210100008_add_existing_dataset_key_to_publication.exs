defmodule DataAggregator.Repo.Migrations.AddExistingDatasetKeyToPublication do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:publications) do
      add :existing_dataset_key, :text
    end
  end

  def down do
    alter table(:publications) do
      remove :existing_dataset_key
    end
  end
end
