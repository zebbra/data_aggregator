defmodule DataAggregator.Repo.Migrations.InitialMigration do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:static_assets, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("uuid_generate_v7()"), primary_key: true
      add :url, :text, null: false
      add :metaData, :map
      add :inserted_at, :utc_datetime_usec, null: false, default: fragment("now()")
      add :updated_at, :utc_datetime_usec, null: false, default: fragment("now()")
      add :dataset_id, :uuid
    end

    create table(:providers, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("uuid_generate_v7()"), primary_key: true
      add :name, :text, null: false
      add :address, :text, null: false
      add :inserted_at, :utc_datetime_usec, null: false, default: fragment("now()")
      add :updated_at, :utc_datetime_usec, null: false, default: fragment("now()")
    end

    create table(:imports, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("uuid_generate_v7()"), primary_key: true
      add :url, :text, null: false
      add :metaData, :map
      add :inserted_at, :utc_datetime_usec, null: false, default: fragment("now()")
      add :updated_at, :utc_datetime_usec, null: false, default: fragment("now()")
      add :dataset_id, :uuid
    end

    create table(:datasets, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("uuid_generate_v7()"), primary_key: true
    end

    alter table(:static_assets) do
      modify :dataset_id,
             references(:datasets,
               column: :id,
               name: "static_assets_dataset_id_fkey",
               type: :uuid,
               prefix: "public"
             )
    end

    alter table(:imports) do
      modify :dataset_id,
             references(:datasets,
               column: :id,
               name: "imports_dataset_id_fkey",
               type: :uuid,
               prefix: "public"
             )
    end

    alter table(:datasets) do
      add :unique_id, :text, null: false
      add :name, :text, null: false
      add :metaData, :map
      add :version, :bigint, null: false
      add :inserted_at, :utc_datetime_usec, null: false, default: fragment("now()")
      add :updated_at, :utc_datetime_usec, null: false, default: fragment("now()")
      add :collection_id, :uuid
    end

    create table(:collections, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("uuid_generate_v7()"), primary_key: true
    end

    alter table(:datasets) do
      modify :collection_id,
             references(:collections,
               column: :id,
               name: "datasets_collection_id_fkey",
               type: :uuid,
               prefix: "public"
             )
    end

    alter table(:collections) do
      add :name, :text, null: false
      add :metaData, :map
      add :inserted_at, :utc_datetime_usec, null: false, default: fragment("now()")
      add :updated_at, :utc_datetime_usec, null: false, default: fragment("now()")

      add :provider_id,
          references(:providers,
            column: :id,
            name: "collections_provider_id_fkey",
            type: :uuid,
            prefix: "public"
          )
    end
  end

  def down do
    drop constraint(:collections, "collections_provider_id_fkey")

    alter table(:collections) do
      remove :provider_id
      remove :updated_at
      remove :inserted_at
      remove :metaData
      remove :name
    end

    drop constraint(:datasets, "datasets_collection_id_fkey")

    alter table(:datasets) do
      modify :collection_id, :uuid
    end

    drop table(:collections)

    alter table(:datasets) do
      remove :collection_id
      remove :updated_at
      remove :inserted_at
      remove :version
      remove :metaData
      remove :name
      remove :unique_id
    end

    drop constraint(:imports, "imports_dataset_id_fkey")

    alter table(:imports) do
      modify :dataset_id, :uuid
    end

    drop constraint(:static_assets, "static_assets_dataset_id_fkey")

    alter table(:static_assets) do
      modify :dataset_id, :uuid
    end

    drop table(:datasets)

    drop table(:imports)

    drop table(:providers)

    drop table(:static_assets)
  end
end
