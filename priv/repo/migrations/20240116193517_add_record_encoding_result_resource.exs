defmodule DataAggregator.Repo.Migrations.AddRecordEncodingResultResource do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:record_encoding_results, primary_key: false) do
      add :id, :uuid, null: false, primary_key: true
      add :input, :map, null: false
      add :output, :map, null: false
      add :message, :text
      add :catalog, :text, null: false
      add :state, :text, null: false
      add :inserted_at, :utc_datetime_usec, null: false, default: fragment("now()")
      add :updated_at, :utc_datetime_usec, null: false, default: fragment("now()")

      add :record_id,
          references(:records,
            column: :id,
            name: "record_encoding_results_record_id_fkey",
            type: :uuid,
            prefix: "public"
          )
    end
  end

  def down do
    drop constraint(:record_encoding_results, "record_encoding_results_record_id_fkey")

    drop table(:record_encoding_results)
  end
end
