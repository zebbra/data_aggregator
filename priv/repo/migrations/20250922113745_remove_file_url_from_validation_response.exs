defmodule DataAggregator.Repo.Migrations.RemoveFileUrlFromValidationResponse do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:validation_responses) do
      remove :file_url
    end
  end

  def down do
    alter table(:validation_responses) do
      add :file_url, :text, null: false
    end
  end
end
