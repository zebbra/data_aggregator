defmodule DataAggregator.Repo.Migrations.AddAttributeBasedMultitenancyToRecordEncodingResults do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    drop constraint(:record_encoding_results, "record_encoding_results_record_id_fkey")

    alter table(:record_encoding_results) do
      add :collection_id,
          references(:collections,
            column: :id,
            name: "record_encoding_results_collection_id_fkey",
            type: :uuid,
            prefix: "public",
            on_delete: :delete_all,
            on_update: :update_all
          )
    end

    execute "UPDATE record_encoding_results SET collection_id = records.collection_id FROM records WHERE record_encoding_results.record_id = records.id"

    alter table(:record_encoding_results) do
      modify :collection_id, :uuid, null: false

      modify :record_id,
             references(:records,
               column: :id,
               with: [collection_id: :collection_id],
               name: "record_encoding_results_record_id_fkey",
               type: :uuid,
               prefix: "public",
               on_delete: :delete_all,
               on_update: :update_all
             ),
             null: false
    end
  end

  def down do
    drop constraint(:record_encoding_results, "record_encoding_results_collection_id_fkey")

    drop constraint(:record_encoding_results, "record_encoding_results_record_id_fkey")

    alter table(:record_encoding_results) do
      modify :record_id,
             references(:records,
               column: :id,
               name: "record_encoding_results_record_id_fkey",
               type: :uuid,
               prefix: "public",
               on_delete: :delete_all,
               on_update: :update_all
             ),
             null: true

      remove :collection_id
    end
  end
end
