defmodule DataAggregator.Repo.Migrations.ImportFileColumns do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:import_files) do
      remove :columns
      add :columns, {:array, :map}
    end
  end

  def down do
    alter table(:import_files) do
      remove :columns
      add :columns, {:array, :text}
    end
  end
end
