defmodule DataAggregator.Repo.Migrations.AddCreatedByAndStartedByUserToImageUploads do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:image_uploads) do
      add :created_by_id,
          references(:users,
            column: :id,
            name: "image_uploads_created_by_id_fkey",
            type: :uuid,
            prefix: "public"
          )

      add :started_by_id,
          references(:users,
            column: :id,
            name: "image_uploads_started_by_id_fkey",
            type: :uuid,
            prefix: "public"
          )
    end
  end

  def down do
    drop constraint(:image_uploads, "image_uploads_created_by_id_fkey")

    drop constraint(:image_uploads, "image_uploads_started_by_id_fkey")

    alter table(:image_uploads) do
      remove :started_by_id
      remove :created_by_id
    end
  end
end
